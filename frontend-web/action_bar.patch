--- src/components/DashboardInteractive.tsx
+++ src/components/DashboardInteractive.tsx
@@ -1070,6 +1070,25 @@
         </div>
       </main>
 
+      {/* Floating Bulk Action Bar */}
+      {selectedCandidateIds.size > 0 && (
+        <div className="fixed bottom-6 left-1/2 -translate-x-1/2 z-40 animate-in slide-in-from-bottom-10 fade-in duration-300">
+          <div className="bg-[#0F172A] border border-[#1E293B] shadow-2xl shadow-blue-500/20 rounded-full px-6 py-3 flex items-center gap-4">
+            <span className="text-slate-300 font-bold text-sm bg-[#1E293B] px-3 py-1 rounded-full">
+              {selectedCandidateIds.size} selected
+            </span>
+            <div className="w-px h-6 bg-[#1E293B]" />
+            <button onClick={handleBulkDownload} disabled={isBulkActionLoading} className="text-sm font-bold text-white bg-[#0EA5E9] hover:bg-[#0284C7] px-4 py-2 rounded-full transition-colors flex items-center gap-2 disabled:opacity-50">
+              {isBulkActionLoading ? <Loader2 className="w-4 h-4 animate-spin" /> : <Download className="w-4 h-4" />}
+              Download CVs
+            </button>
+            <button onClick={handleBulkDelete} disabled={isBulkActionLoading} className="text-sm font-bold text-rose-400 hover:text-white hover:bg-rose-500 px-4 py-2 rounded-full transition-colors flex items-center gap-2 disabled:opacity-50">
+              <Trash2 className="w-4 h-4" />
+              Delete
+            </button>
+          </div>
+        </div>
+      )}
+
       {/* Modals */}
       {selectedCandidate && (
