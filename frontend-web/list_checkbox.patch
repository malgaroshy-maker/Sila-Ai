--- src/components/DashboardInteractive.tsx
+++ src/components/DashboardInteractive.tsx
@@ -871,6 +871,19 @@
               )}
 
               {view === 'list' ? (
+                <div className="px-5 py-2 border-b border-[#1E293B] flex items-center gap-3">
+                  <input 
+                    type="checkbox" 
+                    className="w-4 h-4 rounded border-slate-600 text-[#0EA5E9] focus:ring-[#0EA5E9] bg-slate-800"
+                    checked={filteredResults.length > 0 && selectedCandidateIds.size === filteredResults.length}
+                    onChange={(e) => handleSelectAllCandidates(e.target.checked, filteredResults.map(r => r.candidate_id))}
+                  />
+                  <span className="text-sm font-medium text-slate-400">
+                    {selectedCandidateIds.size > 0 
+                      ? `${selectedCandidateIds.size} selected` 
+                      : 'Select All'}
+                  </span>
+                </div>
                 <div className="p-4 space-y-4 max-h-[700px] overflow-y-auto">
                   {filteredResults.length === 0 ? (
                     <div className="text-center py-12 text-slate-500 font-medium">
@@ -887,6 +900,16 @@
                         {/* Status Backdrop Glow */}
                         <div className={`absolute top-0 end-0 w-48 h-48 opacity-10 bg-gradient-to-br transition-opacity duration-500 group-hover:opacity-30 ${getScoreColor(res.analysis_results?.final_score || 0)} blur-[60px] -translate-y-12 translate-x-12`} />
                         
+                        <div className="absolute top-4 start-4 z-20" onClick={(e) => e.stopPropagation()}>
+                          <input 
+                            type="checkbox" 
+                            className="w-5 h-5 rounded border-slate-600 text-[#0EA5E9] focus:ring-[#0EA5E9] bg-slate-800"
+                            checked={selectedCandidateIds.has(res.candidate_id)}
+                            onChange={(e) => handleSelectCandidate(res.candidate_id, e.target.checked)}
+                            onPointerDown={(e) => e.stopPropagation()}
+                          />
+                        </div>
+
                         <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 relative z-10">
-                          <div className="flex-1 min-w-0">
+                          <div className="flex-1 min-w-0 ps-8">
                             <div className="flex items-center gap-3 mb-2 flex-wrap">
                               <h3 className="font-black text-xl text-white tracking-tight truncate drop-shadow-sm">
